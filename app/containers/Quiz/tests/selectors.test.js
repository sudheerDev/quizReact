// import { fromJS } from 'immutable';
// import { makeSelectQuizDomain } from '../selectors';

// const selector = makeSelectQuizDomain();

describe('makeSelectQuizDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
