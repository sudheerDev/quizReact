// import { fromJS } from 'immutable';
// import { makeSelectResultsDomain } from '../selectors';

// const selector = makeSelectResultsDomain();

describe('makeSelectResultsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
